// import css

// import modules
import {$, jQuery} from "jquery";

window.$ = $;
window.jQuery = jQuery; 
window.shared = require("./scripts/shared.js");
window.log = function(){console.log("error");};

  // "scripts": {
  //   "test": "echo \"Error: no test specified\" && exit 1"
  // }